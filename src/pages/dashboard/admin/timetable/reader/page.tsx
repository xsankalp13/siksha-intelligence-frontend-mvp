import { TimetableReader } from "@/features/academics/timetable_management/components/TimetableReader";

export default function AdminTimetableReaderPage() {
    return (
        <div className="p-6 h-full flex flex-col">
            <TimetableReader />
        </div>
    );
}
